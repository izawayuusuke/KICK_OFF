<% if user_signed_in? && current_user.admin == true %>
<div class="row">
  <div class="col-8">
    <%= render 'leagues' %>
  </div>

  <div class="col-4">
    <%= render 'leagues/form' %>

    <%= render 'teams/form' %>
  </div>
</div>

<% else %>
  <%= render 'leagues' %>
<% end %>
