<%= @player.name %>

<%= form_for [@player, @belong] do |f| %>
  <%= f.label :team_name %>
  <%= f.collection_select :team_id, Team.all, :id, :name %>

  <%= f.submit "作成する" %>
<% end %>

<% @player.teams.each do |team| %>
  <%= team.name %>
<% end %>
