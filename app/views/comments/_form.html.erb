<%= form_with model:[@post, @comment], builder: FormHelper::CustomFormBuilder do |f| %>
  <div class="field">
    <%= f.text_area :comment, rows: "1" %>
  </div>

  <div class="actions">
    <%= f.submit "コメントする", class: "btn btn-outline-primary" %>
  </div>
<% end %>
