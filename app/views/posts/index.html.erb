<div class="row">
  <div class="col-lg-4">
    <%= render 'users/profile_image', user: current_user %>
    <%= current_user.name %>
    <%= render 'new_post' %>
  </div>
  <div class="col-lg-8">
    <%= render 'posts', posts: @posts %>
  </div>
</div>