<div class="row">
  <div class="col-4">
    <%= render 'users/profile', user: current_user %>
    <div class="font-name-bg">
      <%= current_user.name %>
    </div>
    <%= render 'new_post' %>
  </div>
  <div class="col-8">
    <%= render 'posts', posts: @posts %>
  </div>
</div>
